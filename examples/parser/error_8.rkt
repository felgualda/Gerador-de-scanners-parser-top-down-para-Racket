e(define (processa-sistema nota frequencia)
    (if (>= nota 7)
        (if (>= frequencia 75)
            "aprovado"
            (define status-erro "falha" "revisar")
        )
        "reprovado"
    )
)

)

(define (verificar-fim)
    (if #t
        "encerrado"
    )
)