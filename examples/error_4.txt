(define (valida-estado x)
    (if #t
        (define status "ativo")
        (if #f
            (define y 10)
            (define z 20)
            "vazamento"
        )
    )
    (define 404 "not-found")